# Enforce reproducible installs in all environments, including CI.
# pnpm-lock.yaml is committed to the repo — do not run pnpm install
# without committing the updated lockfile afterwards.
frozen-lockfile=true
